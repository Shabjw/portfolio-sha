# Shana Bhojwani Portfolio

One-page portfolio built with Next.js, TypeScript, Tailwind CSS, Framer Motion and modular editable data.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Edit content

Most portfolio content lives in `data/portfolioData.ts`.
